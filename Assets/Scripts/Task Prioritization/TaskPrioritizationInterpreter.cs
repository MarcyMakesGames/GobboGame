using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TaskPrioritizationInterpreter 
{
    public void GetTaskPrioritization(PawnController controller)
    {
        if (controller == null) { return; }
    }
}
